# Migration `watch-20191023163018`

This migration has been generated by Tom Houlé at 10/23/2019, 4:30:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Dog" (
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "photoUrl" TEXT NOT NULL DEFAULT ''  ,
  "score" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Dog" ("id","name","score") SELECT "id","name","score" from "Dog"

DROP TABLE "lift"."Dog";

ALTER TABLE "lift"."new_Dog" RENAME TO "Dog";

CREATE UNIQUE INDEX "lift"."Dog.name" ON "Dog"("name")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191023163018
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,29 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model Dog {
+  id         String      @default(cuid()) @id
+  name       String      @unique
+  attributes Attribute[]
+  score      Int
+  photoUrl   String
+}
+
+model Attribute {
+  id                String            @default(cuid()) @id
+  attributeTemplate AttributeTemplate
+  value             String
+}
+
+model AttributeTemplate {
+  id          String   @default(cuid()) @id
+  name        String   @unique
+  description String
+  choices     String[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191023163018)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191023163018'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
